#!/bin/sh
# Author: cipherodio
# Description: Skip audio notification for specific senders

set -eu

case ${1:-} in
audiobar | brightbar | backlightkey | mpcnotify | Signal)
    exit 0
    ;;
esac

sound="$HOME/.local/share/audio/beep.wav"
paplay "$sound"
