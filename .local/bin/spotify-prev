#!/usr/bin/env bash
# Author: cipherodio
# Description: Go to previous track in Spotify via MPRIS

set -euo pipefail

dbus-send --print-reply \
    --dest=org.mpris.MediaPlayer2.spotify \
    /org/mpris/MediaPlayer2 \
    org.mpris.MediaPlayer2.Player.Previous \
    >/dev/null 2>&1
