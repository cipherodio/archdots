#!/bin/sh
# provides SUDO_ASKPASS variable, used as pass prompt if needed

# dmenu -fn Monospace-18 -P -p "$1" <&-

pass=$(dmenu -fn 'Monospace-18' -P -p "$1" <&-)
[ -n "$pass" ] || exit 1
printf '%s\n' "$pass"

# Last Modified: Sun, 11 Jan 2026 12:55:09 PM
