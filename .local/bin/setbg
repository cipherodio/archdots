#!/bin/sh
# Set wallpaper (stored at $XDG_DATA_HOME/bg)

bgloc="${XDG_DATA_HOME:-$HOME/.local/share}/bg"

if [ -f "$1" ]; then
    cp -f "$1" "$bgloc"
    notify-send -i "$bgloc" " Wallpaper changed."
fi

xwallpaper --zoom "$bgloc"

# Last Modified: Sun, 11 Jan 2026 05:44:21 PM
