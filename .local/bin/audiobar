#!/usr/bin/env bash
# Author: cipherodio
# Description: Show volume change as a dunst progress bar using wpctl

set -euo pipefail

sink="@DEFAULT_AUDIO_SINK@"
action=${1:-0}
msg_id=991049

if [[ "$action" == "toggle" ]]; then
    wpctl set-mute "$sink" toggle
else
    if [[ "$action" =~ ^- ]]; then
        change="${action#-}%-"
    elif [[ "$action" =~ ^\+ ]]; then
        change="${action#+}%+"
    else
        change="${action}%+"
    fi
    wpctl set-volume "$sink" "$change"
    wpctl set-mute "$sink" 0
fi

vol_raw=$(wpctl get-volume "$sink")
current_vol=$(echo "$vol_raw" | awk '{print $2 * 100}' | xargs printf "%.0f")

if [[ "$vol_raw" == *"[MUTED]"* ]]; then
    dunstify -a "audiobar" -u low -r "$msg_id" "Muted"
else
    dunstify -a "audiobar" -u low -r "$msg_id" \
        -h "int:value:$current_vol" \
        "Volume: ${current_vol}%"
fi
