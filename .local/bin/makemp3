#!/bin/sh
# Author: cipherodio
# Description: Convert input media file to MP3 with date-based filename

set -eu

if [ $# -ne 1 ]; then
    printf "Usage: makemp3 <input-file>\n"
    exit 1
fi

input=$1
output=$(date '+%m-%d-%y').mp3

ffmpeg -i "$input" "$output" >/dev/null 2>&1
